{
  "name": "attitude-determination-control-system-of-cubesat",
  "private": true,
  "description": "CubeSat ADCS project — includes Puppeteer snapshots for UI vs Figma comparison in Cursor.",
  "bin": {
    "screenshot-local": "./scripts/screenshot.mjs"
  },
  "scripts": {
    "screenshot": "node scripts/screenshot.mjs",
    "puppeteer": "node scripts/screenshot.mjs",
    "puppeteer:install": "puppeteer browsers install chrome",
    "dev:web": "npm run dev --prefix web",
    "build:web": "npm run build --prefix web",
    "preview:web": "npm run preview --prefix web",
    "screenshot:stitch": "node scripts/screenshot-stitch-routes.mjs"
  },
  "devDependencies": {
    "puppeteer": "^24.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
